NULL =

pygioverridesdir = $(PYGI_OVERRIDES_DIR)
pygioverrides_PYTHON = \
        Unity.py

scopesdir = $(datadir)/unity-scopes
scopes_PYTHON = scope-runner-dbus.py

EXTRA_DIST = \
	Unity.py \
	scope-runner-dbus.py \
	$(NULL)

