/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "dbus-powerd.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

#ifdef G_ENABLE_DEBUG
#define g_marshal_value_peek_boolean(v)  g_value_get_boolean (v)
#define g_marshal_value_peek_char(v)     g_value_get_schar (v)
#define g_marshal_value_peek_uchar(v)    g_value_get_uchar (v)
#define g_marshal_value_peek_int(v)      g_value_get_int (v)
#define g_marshal_value_peek_uint(v)     g_value_get_uint (v)
#define g_marshal_value_peek_long(v)     g_value_get_long (v)
#define g_marshal_value_peek_ulong(v)    g_value_get_ulong (v)
#define g_marshal_value_peek_int64(v)    g_value_get_int64 (v)
#define g_marshal_value_peek_uint64(v)   g_value_get_uint64 (v)
#define g_marshal_value_peek_enum(v)     g_value_get_enum (v)
#define g_marshal_value_peek_flags(v)    g_value_get_flags (v)
#define g_marshal_value_peek_float(v)    g_value_get_float (v)
#define g_marshal_value_peek_double(v)   g_value_get_double (v)
#define g_marshal_value_peek_string(v)   (char*) g_value_get_string (v)
#define g_marshal_value_peek_param(v)    g_value_get_param (v)
#define g_marshal_value_peek_boxed(v)    g_value_get_boxed (v)
#define g_marshal_value_peek_pointer(v)  g_value_get_pointer (v)
#define g_marshal_value_peek_object(v)   g_value_get_object (v)
#define g_marshal_value_peek_variant(v)  g_value_get_variant (v)
#else /* !G_ENABLE_DEBUG */
/* WARNING: This code accesses GValues directly, which is UNSUPPORTED API.
 *          Do not access GValues directly in your code. Instead, use the
 *          g_value_get_*() functions
 */
#define g_marshal_value_peek_boolean(v)  (v)->data[0].v_int
#define g_marshal_value_peek_char(v)     (v)->data[0].v_int
#define g_marshal_value_peek_uchar(v)    (v)->data[0].v_uint
#define g_marshal_value_peek_int(v)      (v)->data[0].v_int
#define g_marshal_value_peek_uint(v)     (v)->data[0].v_uint
#define g_marshal_value_peek_long(v)     (v)->data[0].v_long
#define g_marshal_value_peek_ulong(v)    (v)->data[0].v_ulong
#define g_marshal_value_peek_int64(v)    (v)->data[0].v_int64
#define g_marshal_value_peek_uint64(v)   (v)->data[0].v_uint64
#define g_marshal_value_peek_enum(v)     (v)->data[0].v_long
#define g_marshal_value_peek_flags(v)    (v)->data[0].v_ulong
#define g_marshal_value_peek_float(v)    (v)->data[0].v_float
#define g_marshal_value_peek_double(v)   (v)->data[0].v_double
#define g_marshal_value_peek_string(v)   (v)->data[0].v_pointer
#define g_marshal_value_peek_param(v)    (v)->data[0].v_pointer
#define g_marshal_value_peek_boxed(v)    (v)->data[0].v_pointer
#define g_marshal_value_peek_pointer(v)  (v)->data[0].v_pointer
#define g_marshal_value_peek_object(v)   (v)->data[0].v_pointer
#define g_marshal_value_peek_variant(v)  (v)->data[0].v_pointer
#endif /* !G_ENABLE_DEBUG */

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_INT (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectStringIntFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        const gchar *arg_name,
        gint arg_state,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectStringIntFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 4);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectStringIntFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_string (param_values + 2),
              g_marshal_value_peek_int (param_values + 3),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectStringFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        const gchar *arg_cookie,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectStringFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 3);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectStringFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_string (param_values + 2),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_UINT64 (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectStringUint64Func)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        const gchar *arg_name,
        guint64 arg_time,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectStringUint64Func callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 4);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectStringUint64Func)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_string (param_values + 2),
              g_marshal_value_peek_uint64 (param_values + 3),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_INT (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectIntFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        gint arg_state,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectIntFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 3);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectIntFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_int (param_values + 2),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_BOOLEAN (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectBooleanFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        gboolean arg_enable,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectBooleanFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 3);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectBooleanFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_boolean (param_values + 2),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 2);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              data2);

  g_value_set_boolean (return_value, v_return);
}

/* ------------------------------------------------------------------------
 * Code for interface com.canonical.powerd
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:DbusPowerd
 * @title: DbusPowerd
 * @short_description: Generated C code for the com.canonical.powerd D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-canonical-powerd.top_of_page">com.canonical.powerd</link> D-Bus interface in C.
 */

enum
{
  DBUS__POWERD_SYS_POWER_STATE_CHANGE,
  DBUS__POWERD_WAKEUP,
};

static unsigned DBUS__POWERD_SIGNALS[2] = { 0 };

/* ---- Introspection data for com.canonical.powerd ---- */

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_request_sys_state_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_request_sys_state_IN_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_request_sys_state_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_request_sys_state_IN_ARG_name.parent_struct,
  &_dbus_powerd_method_info_request_sys_state_IN_ARG_state.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_request_sys_state_OUT_ARG_cookie =
{
  {
    -1,
    (gchar *) "cookie",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_request_sys_state_OUT_ARG_pointers[] =
{
  &_dbus_powerd_method_info_request_sys_state_OUT_ARG_cookie.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_request_sys_state =
{
  {
    -1,
    (gchar *) "requestSysState",
    (GDBusArgInfo **) &_dbus_powerd_method_info_request_sys_state_IN_ARG_pointers,
    (GDBusArgInfo **) &_dbus_powerd_method_info_request_sys_state_OUT_ARG_pointers,
    NULL
  },
  "handle-request-sys-state",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_clear_sys_state_IN_ARG_cookie =
{
  {
    -1,
    (gchar *) "cookie",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_clear_sys_state_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_clear_sys_state_IN_ARG_cookie.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_clear_sys_state =
{
  {
    -1,
    (gchar *) "clearSysState",
    (GDBusArgInfo **) &_dbus_powerd_method_info_clear_sys_state_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-clear-sys-state",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_request_wakeup_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_request_wakeup_IN_ARG_time =
{
  {
    -1,
    (gchar *) "time",
    (gchar *) "t",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_request_wakeup_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_request_wakeup_IN_ARG_name.parent_struct,
  &_dbus_powerd_method_info_request_wakeup_IN_ARG_time.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_request_wakeup_OUT_ARG_cookie =
{
  {
    -1,
    (gchar *) "cookie",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_request_wakeup_OUT_ARG_pointers[] =
{
  &_dbus_powerd_method_info_request_wakeup_OUT_ARG_cookie.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_request_wakeup =
{
  {
    -1,
    (gchar *) "requestWakeup",
    (GDBusArgInfo **) &_dbus_powerd_method_info_request_wakeup_IN_ARG_pointers,
    (GDBusArgInfo **) &_dbus_powerd_method_info_request_wakeup_OUT_ARG_pointers,
    NULL
  },
  "handle-request-wakeup",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_enable_proximity_handling_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_enable_proximity_handling_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_enable_proximity_handling_IN_ARG_name.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_enable_proximity_handling =
{
  {
    -1,
    (gchar *) "enableProximityHandling",
    (GDBusArgInfo **) &_dbus_powerd_method_info_enable_proximity_handling_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-enable-proximity-handling",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_disable_proximity_handling_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_disable_proximity_handling_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_disable_proximity_handling_IN_ARG_name.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_disable_proximity_handling =
{
  {
    -1,
    (gchar *) "disableProximityHandling",
    (GDBusArgInfo **) &_dbus_powerd_method_info_disable_proximity_handling_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-disable-proximity-handling",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_clear_wakeup_IN_ARG_cookie =
{
  {
    -1,
    (gchar *) "cookie",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_clear_wakeup_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_clear_wakeup_IN_ARG_cookie.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_clear_wakeup =
{
  {
    -1,
    (gchar *) "clearWakeup",
    (GDBusArgInfo **) &_dbus_powerd_method_info_clear_wakeup_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-clear-wakeup",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_register_client_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_register_client_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_register_client_IN_ARG_name.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_register_client =
{
  {
    -1,
    (gchar *) "registerClient",
    (GDBusArgInfo **) &_dbus_powerd_method_info_register_client_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-register-client",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_unregister_client_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_unregister_client_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_unregister_client_IN_ARG_name.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_unregister_client =
{
  {
    -1,
    (gchar *) "unregisterClient",
    (GDBusArgInfo **) &_dbus_powerd_method_info_unregister_client_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-unregister-client",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_ack_state_change_IN_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_ack_state_change_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_ack_state_change_IN_ARG_state.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_ack_state_change =
{
  {
    -1,
    (gchar *) "ackStateChange",
    (GDBusArgInfo **) &_dbus_powerd_method_info_ack_state_change_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-ack-state-change",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_user_autobrightness_enable_IN_ARG_enable =
{
  {
    -1,
    (gchar *) "enable",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_user_autobrightness_enable_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_user_autobrightness_enable_IN_ARG_enable.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_user_autobrightness_enable =
{
  {
    -1,
    (gchar *) "userAutobrightnessEnable",
    (GDBusArgInfo **) &_dbus_powerd_method_info_user_autobrightness_enable_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-user-autobrightness-enable",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_get_brightness_params_OUT_ARG_params =
{
  {
    -1,
    (gchar *) "params",
    (gchar *) "(iiiib)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_get_brightness_params_OUT_ARG_pointers[] =
{
  &_dbus_powerd_method_info_get_brightness_params_OUT_ARG_params.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_get_brightness_params =
{
  {
    -1,
    (gchar *) "getBrightnessParams",
    NULL,
    (GDBusArgInfo **) &_dbus_powerd_method_info_get_brightness_params_OUT_ARG_pointers,
    NULL
  },
  "handle-get-brightness-params",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_set_user_brightness_IN_ARG_brightness =
{
  {
    -1,
    (gchar *) "brightness",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_set_user_brightness_IN_ARG_pointers[] =
{
  &_dbus_powerd_method_info_set_user_brightness_IN_ARG_brightness.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_set_user_brightness =
{
  {
    -1,
    (gchar *) "setUserBrightness",
    (GDBusArgInfo **) &_dbus_powerd_method_info_set_user_brightness_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-user-brightness",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_list_sys_requests_OUT_ARG_requestList =
{
  {
    -1,
    (gchar *) "requestList",
    (gchar *) "a(ssi)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_list_sys_requests_OUT_ARG_pointers[] =
{
  &_dbus_powerd_method_info_list_sys_requests_OUT_ARG_requestList.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_list_sys_requests =
{
  {
    -1,
    (gchar *) "listSysRequests",
    NULL,
    (GDBusArgInfo **) &_dbus_powerd_method_info_list_sys_requests_OUT_ARG_pointers,
    NULL
  },
  "handle-list-sys-requests",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_powerd_method_info_get_sys_request_stats_OUT_ARG_requestStats =
{
  {
    -1,
    (gchar *) "requestStats",
    (gchar *) "a(ssuttt)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_method_info_get_sys_request_stats_OUT_ARG_pointers[] =
{
  &_dbus_powerd_method_info_get_sys_request_stats_OUT_ARG_requestStats.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_powerd_method_info_get_sys_request_stats =
{
  {
    -1,
    (gchar *) "getSysRequestStats",
    NULL,
    (GDBusArgInfo **) &_dbus_powerd_method_info_get_sys_request_stats_OUT_ARG_pointers,
    NULL
  },
  "handle-get-sys-request-stats",
  FALSE
};

static const GDBusMethodInfo * const _dbus_powerd_method_info_pointers[] =
{
  &_dbus_powerd_method_info_request_sys_state.parent_struct,
  &_dbus_powerd_method_info_clear_sys_state.parent_struct,
  &_dbus_powerd_method_info_request_wakeup.parent_struct,
  &_dbus_powerd_method_info_enable_proximity_handling.parent_struct,
  &_dbus_powerd_method_info_disable_proximity_handling.parent_struct,
  &_dbus_powerd_method_info_clear_wakeup.parent_struct,
  &_dbus_powerd_method_info_register_client.parent_struct,
  &_dbus_powerd_method_info_unregister_client.parent_struct,
  &_dbus_powerd_method_info_ack_state_change.parent_struct,
  &_dbus_powerd_method_info_user_autobrightness_enable.parent_struct,
  &_dbus_powerd_method_info_get_brightness_params.parent_struct,
  &_dbus_powerd_method_info_set_user_brightness.parent_struct,
  &_dbus_powerd_method_info_list_sys_requests.parent_struct,
  &_dbus_powerd_method_info_get_sys_request_stats.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _dbus_powerd_signal_info_sys_power_state_change_ARG_sysState =
{
  {
    -1,
    (gchar *) "sysState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_powerd_signal_info_sys_power_state_change_ARG_pointers[] =
{
  &_dbus_powerd_signal_info_sys_power_state_change_ARG_sysState.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _dbus_powerd_signal_info_sys_power_state_change =
{
  {
    -1,
    (gchar *) "SysPowerStateChange",
    (GDBusArgInfo **) &_dbus_powerd_signal_info_sys_power_state_change_ARG_pointers,
    NULL
  },
  "sys-power-state-change"
};

static const _ExtendedGDBusSignalInfo _dbus_powerd_signal_info_wakeup =
{
  {
    -1,
    (gchar *) "Wakeup",
    NULL,
    NULL
  },
  "wakeup"
};

static const GDBusSignalInfo * const _dbus_powerd_signal_info_pointers[] =
{
  &_dbus_powerd_signal_info_sys_power_state_change.parent_struct,
  &_dbus_powerd_signal_info_wakeup.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _dbus_powerd_property_info_brightness =
{
  {
    -1,
    (gchar *) "brightness",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "brightness",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _dbus_powerd_property_info_pointers[] =
{
  &_dbus_powerd_property_info_brightness.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _dbus_powerd_interface_info =
{
  {
    -1,
    (gchar *) "com.canonical.powerd",
    (GDBusMethodInfo **) &_dbus_powerd_method_info_pointers,
    (GDBusSignalInfo **) &_dbus_powerd_signal_info_pointers,
    (GDBusPropertyInfo **) &_dbus_powerd_property_info_pointers,
    NULL
  },
  "powerd",
};


/**
 * dbus_powerd_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-canonical-powerd.top_of_page">com.canonical.powerd</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
dbus_powerd_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_dbus_powerd_interface_info.parent_struct;
}

/**
 * dbus_powerd_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #DbusPowerd interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
dbus_powerd_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "brightness");
  return property_id_begin - 1;
}


inline static void
dbus_powerd_signal_marshal_sys_power_state_change (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  g_cclosure_marshal_VOID__INT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_signal_marshal_wakeup (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  g_cclosure_marshal_VOID__VOID (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_request_sys_state (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_INT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_clear_sys_state (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_request_wakeup (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_UINT64 (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_enable_proximity_handling (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_disable_proximity_handling (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_clear_wakeup (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_register_client (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_unregister_client (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_ack_state_change (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_INT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_user_autobrightness_enable (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_BOOLEAN (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_get_brightness_params (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_set_user_brightness (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_INT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_list_sys_requests (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_powerd_method_marshal_get_sys_request_stats (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}


/**
 * DbusPowerd:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-canonical-powerd.top_of_page">com.canonical.powerd</link>.
 */

/**
 * DbusPowerdIface:
 * @parent_iface: The parent interface.
 * @handle_ack_state_change: Handler for the #DbusPowerd::handle-ack-state-change signal.
 * @handle_clear_sys_state: Handler for the #DbusPowerd::handle-clear-sys-state signal.
 * @handle_clear_wakeup: Handler for the #DbusPowerd::handle-clear-wakeup signal.
 * @handle_disable_proximity_handling: Handler for the #DbusPowerd::handle-disable-proximity-handling signal.
 * @handle_enable_proximity_handling: Handler for the #DbusPowerd::handle-enable-proximity-handling signal.
 * @handle_get_brightness_params: Handler for the #DbusPowerd::handle-get-brightness-params signal.
 * @handle_get_sys_request_stats: Handler for the #DbusPowerd::handle-get-sys-request-stats signal.
 * @handle_list_sys_requests: Handler for the #DbusPowerd::handle-list-sys-requests signal.
 * @handle_register_client: Handler for the #DbusPowerd::handle-register-client signal.
 * @handle_request_sys_state: Handler for the #DbusPowerd::handle-request-sys-state signal.
 * @handle_request_wakeup: Handler for the #DbusPowerd::handle-request-wakeup signal.
 * @handle_set_user_brightness: Handler for the #DbusPowerd::handle-set-user-brightness signal.
 * @handle_unregister_client: Handler for the #DbusPowerd::handle-unregister-client signal.
 * @handle_user_autobrightness_enable: Handler for the #DbusPowerd::handle-user-autobrightness-enable signal.
 * @get_brightness: Getter for the #DbusPowerd:brightness property.
 * @sys_power_state_change: Handler for the #DbusPowerd::sys-power-state-change signal.
 * @wakeup: Handler for the #DbusPowerd::wakeup signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-canonical-powerd.top_of_page">com.canonical.powerd</link>.
 */

typedef DbusPowerdIface DbusPowerdInterface;
G_DEFINE_INTERFACE (DbusPowerd, dbus_powerd, G_TYPE_OBJECT)

static void
dbus_powerd_default_init (DbusPowerdIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * DbusPowerd::handle-request-sys-state:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_state: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.requestSysState">requestSysState()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_request_sys_state() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request-sys-state",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_request_sys_state),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_request_sys_state,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT);

  /**
   * DbusPowerd::handle-clear-sys-state:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_cookie: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.clearSysState">clearSysState()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_clear_sys_state() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-clear-sys-state",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_clear_sys_state),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_clear_sys_state,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * DbusPowerd::handle-request-wakeup:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   * @arg_time: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.requestWakeup">requestWakeup()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_request_wakeup() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-request-wakeup",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_request_wakeup),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_request_wakeup,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_UINT64);

  /**
   * DbusPowerd::handle-enable-proximity-handling:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.enableProximityHandling">enableProximityHandling()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_enable_proximity_handling() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-enable-proximity-handling",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_enable_proximity_handling),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_enable_proximity_handling,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * DbusPowerd::handle-disable-proximity-handling:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.disableProximityHandling">disableProximityHandling()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_disable_proximity_handling() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-disable-proximity-handling",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_disable_proximity_handling),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_disable_proximity_handling,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * DbusPowerd::handle-clear-wakeup:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_cookie: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.clearWakeup">clearWakeup()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_clear_wakeup() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-clear-wakeup",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_clear_wakeup),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_clear_wakeup,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * DbusPowerd::handle-register-client:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.registerClient">registerClient()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_register_client() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-register-client",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_register_client),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_register_client,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * DbusPowerd::handle-unregister-client:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.unregisterClient">unregisterClient()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_unregister_client() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-unregister-client",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_unregister_client),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_unregister_client,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * DbusPowerd::handle-ack-state-change:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_state: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.ackStateChange">ackStateChange()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_ack_state_change() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-ack-state-change",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_ack_state_change),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_ack_state_change,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT);

  /**
   * DbusPowerd::handle-user-autobrightness-enable:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_enable: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.userAutobrightnessEnable">userAutobrightnessEnable()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_user_autobrightness_enable() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-user-autobrightness-enable",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_user_autobrightness_enable),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_user_autobrightness_enable,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * DbusPowerd::handle-get-brightness-params:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.getBrightnessParams">getBrightnessParams()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_get_brightness_params() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-brightness-params",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_get_brightness_params),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_get_brightness_params,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusPowerd::handle-set-user-brightness:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_brightness: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.setUserBrightness">setUserBrightness()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_set_user_brightness() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-user-brightness",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_set_user_brightness),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_set_user_brightness,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT);

  /**
   * DbusPowerd::handle-list-sys-requests:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.listSysRequests">listSysRequests()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_list_sys_requests() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-list-sys-requests",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_list_sys_requests),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_list_sys_requests,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusPowerd::handle-get-sys-request-stats:
   * @object: A #DbusPowerd.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-canonical-powerd.getSysRequestStats">getSysRequestStats()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_powerd_complete_get_sys_request_stats() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-sys-request-stats",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusPowerdIface, handle_get_sys_request_stats),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_powerd_method_marshal_get_sys_request_stats,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * DbusPowerd::sys-power-state-change:
   * @object: A #DbusPowerd.
   * @arg_sysState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-canonical-powerd.SysPowerStateChange">"SysPowerStateChange"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  DBUS__POWERD_SIGNALS[DBUS__POWERD_SYS_POWER_STATE_CHANGE] =
    g_signal_new ("sys-power-state-change",
      G_TYPE_FROM_INTERFACE (iface),
      G_SIGNAL_RUN_LAST,
      G_STRUCT_OFFSET (DbusPowerdIface, sys_power_state_change),
      NULL,
      NULL,
      dbus_powerd_signal_marshal_sys_power_state_change,
      G_TYPE_NONE,
      1, G_TYPE_INT);

  /**
   * DbusPowerd::wakeup:
   * @object: A #DbusPowerd.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-canonical-powerd.Wakeup">"Wakeup"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  DBUS__POWERD_SIGNALS[DBUS__POWERD_WAKEUP] =
    g_signal_new ("wakeup",
      G_TYPE_FROM_INTERFACE (iface),
      G_SIGNAL_RUN_LAST,
      G_STRUCT_OFFSET (DbusPowerdIface, wakeup),
      NULL,
      NULL,
      dbus_powerd_signal_marshal_wakeup,
      G_TYPE_NONE,
      0);

  /* GObject properties for D-Bus properties: */
  /**
   * DbusPowerd:brightness:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-canonical-powerd.brightness">"brightness"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("brightness", "brightness", "brightness", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * dbus_powerd_get_brightness: (skip)
 * @object: A #DbusPowerd.
 *
 * Gets the value of the <link linkend="gdbus-property-com-canonical-powerd.brightness">"brightness"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
dbus_powerd_get_brightness (DbusPowerd *object)
{
  g_return_val_if_fail (DBUS_IS_POWERD (object), 0);

  return DBUS_POWERD_GET_IFACE (object)->get_brightness (object);
}

/**
 * dbus_powerd_set_brightness: (skip)
 * @object: A #DbusPowerd.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-canonical-powerd.brightness">"brightness"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
dbus_powerd_set_brightness (DbusPowerd *object, gint value)
{
  g_object_set (G_OBJECT (object), "brightness", value, NULL);
}

/**
 * dbus_powerd_emit_sys_power_state_change:
 * @object: A #DbusPowerd.
 * @arg_sysState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-canonical-powerd.SysPowerStateChange">"SysPowerStateChange"</link> D-Bus signal.
 */
void
dbus_powerd_emit_sys_power_state_change (
    DbusPowerd *object,
    gint arg_sysState)
{
  g_signal_emit (object, DBUS__POWERD_SIGNALS[DBUS__POWERD_SYS_POWER_STATE_CHANGE], 0, arg_sysState);
}

/**
 * dbus_powerd_emit_wakeup:
 * @object: A #DbusPowerd.
 *
 * Emits the <link linkend="gdbus-signal-com-canonical-powerd.Wakeup">"Wakeup"</link> D-Bus signal.
 */
void
dbus_powerd_emit_wakeup (
    DbusPowerd *object)
{
  g_signal_emit (object, DBUS__POWERD_SIGNALS[DBUS__POWERD_WAKEUP], 0);
}

/**
 * dbus_powerd_call_request_sys_state:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_state: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.requestSysState">requestSysState()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_request_sys_state_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_request_sys_state_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_request_sys_state (
    DbusPowerd *proxy,
    const gchar *arg_name,
    gint arg_state,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "requestSysState",
    g_variant_new ("(si)",
                   arg_name,
                   arg_state),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_request_sys_state_finish:
 * @proxy: A #DbusPowerdProxy.
 * @out_cookie: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_request_sys_state().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_request_sys_state().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_request_sys_state_finish (
    DbusPowerd *proxy,
    gchar **out_cookie,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_cookie);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_request_sys_state_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_state: Argument to pass with the method invocation.
 * @out_cookie: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.requestSysState">requestSysState()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_request_sys_state() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_request_sys_state_sync (
    DbusPowerd *proxy,
    const gchar *arg_name,
    gint arg_state,
    gchar **out_cookie,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "requestSysState",
    g_variant_new ("(si)",
                   arg_name,
                   arg_state),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_cookie);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_clear_sys_state:
 * @proxy: A #DbusPowerdProxy.
 * @arg_cookie: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.clearSysState">clearSysState()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_clear_sys_state_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_clear_sys_state_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_clear_sys_state (
    DbusPowerd *proxy,
    const gchar *arg_cookie,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "clearSysState",
    g_variant_new ("(s)",
                   arg_cookie),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_clear_sys_state_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_clear_sys_state().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_clear_sys_state().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_clear_sys_state_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_clear_sys_state_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_cookie: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.clearSysState">clearSysState()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_clear_sys_state() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_clear_sys_state_sync (
    DbusPowerd *proxy,
    const gchar *arg_cookie,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "clearSysState",
    g_variant_new ("(s)",
                   arg_cookie),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_request_wakeup:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_time: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.requestWakeup">requestWakeup()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_request_wakeup_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_request_wakeup_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_request_wakeup (
    DbusPowerd *proxy,
    const gchar *arg_name,
    guint64 arg_time,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "requestWakeup",
    g_variant_new ("(st)",
                   arg_name,
                   arg_time),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_request_wakeup_finish:
 * @proxy: A #DbusPowerdProxy.
 * @out_cookie: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_request_wakeup().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_request_wakeup().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_request_wakeup_finish (
    DbusPowerd *proxy,
    gchar **out_cookie,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_cookie);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_request_wakeup_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @arg_time: Argument to pass with the method invocation.
 * @out_cookie: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.requestWakeup">requestWakeup()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_request_wakeup() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_request_wakeup_sync (
    DbusPowerd *proxy,
    const gchar *arg_name,
    guint64 arg_time,
    gchar **out_cookie,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "requestWakeup",
    g_variant_new ("(st)",
                   arg_name,
                   arg_time),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_cookie);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_enable_proximity_handling:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.enableProximityHandling">enableProximityHandling()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_enable_proximity_handling_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_enable_proximity_handling_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_enable_proximity_handling (
    DbusPowerd *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "enableProximityHandling",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_enable_proximity_handling_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_enable_proximity_handling().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_enable_proximity_handling().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_enable_proximity_handling_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_enable_proximity_handling_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.enableProximityHandling">enableProximityHandling()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_enable_proximity_handling() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_enable_proximity_handling_sync (
    DbusPowerd *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "enableProximityHandling",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_disable_proximity_handling:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.disableProximityHandling">disableProximityHandling()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_disable_proximity_handling_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_disable_proximity_handling_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_disable_proximity_handling (
    DbusPowerd *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "disableProximityHandling",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_disable_proximity_handling_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_disable_proximity_handling().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_disable_proximity_handling().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_disable_proximity_handling_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_disable_proximity_handling_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.disableProximityHandling">disableProximityHandling()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_disable_proximity_handling() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_disable_proximity_handling_sync (
    DbusPowerd *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "disableProximityHandling",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_clear_wakeup:
 * @proxy: A #DbusPowerdProxy.
 * @arg_cookie: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.clearWakeup">clearWakeup()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_clear_wakeup_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_clear_wakeup_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_clear_wakeup (
    DbusPowerd *proxy,
    const gchar *arg_cookie,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "clearWakeup",
    g_variant_new ("(s)",
                   arg_cookie),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_clear_wakeup_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_clear_wakeup().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_clear_wakeup().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_clear_wakeup_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_clear_wakeup_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_cookie: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.clearWakeup">clearWakeup()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_clear_wakeup() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_clear_wakeup_sync (
    DbusPowerd *proxy,
    const gchar *arg_cookie,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "clearWakeup",
    g_variant_new ("(s)",
                   arg_cookie),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_register_client:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.registerClient">registerClient()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_register_client_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_register_client_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_register_client (
    DbusPowerd *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "registerClient",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_register_client_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_register_client().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_register_client().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_register_client_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_register_client_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.registerClient">registerClient()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_register_client() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_register_client_sync (
    DbusPowerd *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "registerClient",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_unregister_client:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.unregisterClient">unregisterClient()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_unregister_client_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_unregister_client_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_unregister_client (
    DbusPowerd *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "unregisterClient",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_unregister_client_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_unregister_client().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_unregister_client().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_unregister_client_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_unregister_client_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.unregisterClient">unregisterClient()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_unregister_client() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_unregister_client_sync (
    DbusPowerd *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "unregisterClient",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_ack_state_change:
 * @proxy: A #DbusPowerdProxy.
 * @arg_state: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.ackStateChange">ackStateChange()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_ack_state_change_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_ack_state_change_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_ack_state_change (
    DbusPowerd *proxy,
    gint arg_state,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ackStateChange",
    g_variant_new ("(i)",
                   arg_state),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_ack_state_change_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_ack_state_change().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_ack_state_change().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_ack_state_change_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_ack_state_change_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_state: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.ackStateChange">ackStateChange()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_ack_state_change() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_ack_state_change_sync (
    DbusPowerd *proxy,
    gint arg_state,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ackStateChange",
    g_variant_new ("(i)",
                   arg_state),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_user_autobrightness_enable:
 * @proxy: A #DbusPowerdProxy.
 * @arg_enable: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.userAutobrightnessEnable">userAutobrightnessEnable()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_user_autobrightness_enable_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_user_autobrightness_enable_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_user_autobrightness_enable (
    DbusPowerd *proxy,
    gboolean arg_enable,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "userAutobrightnessEnable",
    g_variant_new ("(b)",
                   arg_enable),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_user_autobrightness_enable_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_user_autobrightness_enable().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_user_autobrightness_enable().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_user_autobrightness_enable_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_user_autobrightness_enable_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_enable: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.userAutobrightnessEnable">userAutobrightnessEnable()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_user_autobrightness_enable() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_user_autobrightness_enable_sync (
    DbusPowerd *proxy,
    gboolean arg_enable,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "userAutobrightnessEnable",
    g_variant_new ("(b)",
                   arg_enable),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_get_brightness_params:
 * @proxy: A #DbusPowerdProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.getBrightnessParams">getBrightnessParams()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_get_brightness_params_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_get_brightness_params_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_get_brightness_params (
    DbusPowerd *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getBrightnessParams",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_get_brightness_params_finish:
 * @proxy: A #DbusPowerdProxy.
 * @out_params: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_get_brightness_params().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_get_brightness_params().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_get_brightness_params_finish (
    DbusPowerd *proxy,
    GVariant **out_params,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(iiiib))",
                 out_params);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_get_brightness_params_sync:
 * @proxy: A #DbusPowerdProxy.
 * @out_params: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.getBrightnessParams">getBrightnessParams()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_get_brightness_params() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_get_brightness_params_sync (
    DbusPowerd *proxy,
    GVariant **out_params,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getBrightnessParams",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(iiiib))",
                 out_params);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_set_user_brightness:
 * @proxy: A #DbusPowerdProxy.
 * @arg_brightness: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.setUserBrightness">setUserBrightness()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_set_user_brightness_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_set_user_brightness_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_set_user_brightness (
    DbusPowerd *proxy,
    gint arg_brightness,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setUserBrightness",
    g_variant_new ("(i)",
                   arg_brightness),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_set_user_brightness_finish:
 * @proxy: A #DbusPowerdProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_set_user_brightness().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_set_user_brightness().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_set_user_brightness_finish (
    DbusPowerd *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_set_user_brightness_sync:
 * @proxy: A #DbusPowerdProxy.
 * @arg_brightness: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.setUserBrightness">setUserBrightness()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_set_user_brightness() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_set_user_brightness_sync (
    DbusPowerd *proxy,
    gint arg_brightness,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setUserBrightness",
    g_variant_new ("(i)",
                   arg_brightness),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_list_sys_requests:
 * @proxy: A #DbusPowerdProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.listSysRequests">listSysRequests()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_list_sys_requests_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_list_sys_requests_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_list_sys_requests (
    DbusPowerd *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "listSysRequests",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_list_sys_requests_finish:
 * @proxy: A #DbusPowerdProxy.
 * @out_requestList: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_list_sys_requests().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_list_sys_requests().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_list_sys_requests_finish (
    DbusPowerd *proxy,
    GVariant **out_requestList,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ssi))",
                 out_requestList);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_list_sys_requests_sync:
 * @proxy: A #DbusPowerdProxy.
 * @out_requestList: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.listSysRequests">listSysRequests()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_list_sys_requests() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_list_sys_requests_sync (
    DbusPowerd *proxy,
    GVariant **out_requestList,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "listSysRequests",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ssi))",
                 out_requestList);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_get_sys_request_stats:
 * @proxy: A #DbusPowerdProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.getSysRequestStats">getSysRequestStats()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_call_get_sys_request_stats_finish() to get the result of the operation.
 *
 * See dbus_powerd_call_get_sys_request_stats_sync() for the synchronous, blocking version of this method.
 */
void
dbus_powerd_call_get_sys_request_stats (
    DbusPowerd *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getSysRequestStats",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_powerd_call_get_sys_request_stats_finish:
 * @proxy: A #DbusPowerdProxy.
 * @out_requestStats: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_call_get_sys_request_stats().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_powerd_call_get_sys_request_stats().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_get_sys_request_stats_finish (
    DbusPowerd *proxy,
    GVariant **out_requestStats,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ssuttt))",
                 out_requestStats);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_call_get_sys_request_stats_sync:
 * @proxy: A #DbusPowerdProxy.
 * @out_requestStats: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-canonical-powerd.getSysRequestStats">getSysRequestStats()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_call_get_sys_request_stats() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_powerd_call_get_sys_request_stats_sync (
    DbusPowerd *proxy,
    GVariant **out_requestStats,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getSysRequestStats",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ssuttt))",
                 out_requestStats);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_powerd_complete_request_sys_state:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @cookie: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.requestSysState">requestSysState()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_request_sys_state (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    const gchar *cookie)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   cookie));
}

/**
 * dbus_powerd_complete_clear_sys_state:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.clearSysState">clearSysState()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_clear_sys_state (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_request_wakeup:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @cookie: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.requestWakeup">requestWakeup()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_request_wakeup (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    const gchar *cookie)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   cookie));
}

/**
 * dbus_powerd_complete_enable_proximity_handling:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.enableProximityHandling">enableProximityHandling()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_enable_proximity_handling (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_disable_proximity_handling:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.disableProximityHandling">disableProximityHandling()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_disable_proximity_handling (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_clear_wakeup:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.clearWakeup">clearWakeup()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_clear_wakeup (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_register_client:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.registerClient">registerClient()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_register_client (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_unregister_client:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.unregisterClient">unregisterClient()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_unregister_client (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_ack_state_change:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.ackStateChange">ackStateChange()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_ack_state_change (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_user_autobrightness_enable:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.userAutobrightnessEnable">userAutobrightnessEnable()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_user_autobrightness_enable (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_get_brightness_params:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @params: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.getBrightnessParams">getBrightnessParams()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_get_brightness_params (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GVariant *params)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(iiiib))",
                   params));
}

/**
 * dbus_powerd_complete_set_user_brightness:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.setUserBrightness">setUserBrightness()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_set_user_brightness (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_powerd_complete_list_sys_requests:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @requestList: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.listSysRequests">listSysRequests()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_list_sys_requests (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GVariant *requestList)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(ssi))",
                   requestList));
}

/**
 * dbus_powerd_complete_get_sys_request_stats:
 * @object: A #DbusPowerd.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @requestStats: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-canonical-powerd.getSysRequestStats">getSysRequestStats()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_powerd_complete_get_sys_request_stats (
    DbusPowerd *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GVariant *requestStats)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(ssuttt))",
                   requestStats));
}

/* ------------------------------------------------------------------------ */

/**
 * DbusPowerdProxy:
 *
 * The #DbusPowerdProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusPowerdProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusPowerdProxy.
 */

struct _DbusPowerdProxyPrivate
{
  GData *qdata;
};

static void dbus_powerd_proxy_iface_init (DbusPowerdIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusPowerdProxy, dbus_powerd_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (DbusPowerdProxy)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_POWERD, dbus_powerd_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusPowerdProxy, dbus_powerd_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_POWERD, dbus_powerd_proxy_iface_init))

#endif
static void
dbus_powerd_proxy_finalize (GObject *object)
{
  DbusPowerdProxy *proxy = DBUS_POWERD_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (dbus_powerd_proxy_parent_class)->finalize (object);
}

static void
dbus_powerd_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_powerd_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
dbus_powerd_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface com.canonical.powerd: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
dbus_powerd_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_powerd_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "com.canonical.powerd", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) dbus_powerd_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
dbus_powerd_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_dbus_powerd_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], DBUS_TYPE_POWERD);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_POWERD);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
dbus_powerd_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  DbusPowerdProxy *proxy = DBUS_POWERD_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_powerd_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_powerd_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gint 
dbus_powerd_proxy_get_brightness (DbusPowerd *object)
{
  DbusPowerdProxy *proxy = DBUS_POWERD_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "brightness");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
dbus_powerd_proxy_init (DbusPowerdProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = dbus_powerd_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, DBUS_TYPE_POWERD_PROXY, DbusPowerdProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), dbus_powerd_interface_info ());
}

static void
dbus_powerd_proxy_class_init (DbusPowerdProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = dbus_powerd_proxy_finalize;
  gobject_class->get_property = dbus_powerd_proxy_get_property;
  gobject_class->set_property = dbus_powerd_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = dbus_powerd_proxy_g_signal;
  proxy_class->g_properties_changed = dbus_powerd_proxy_g_properties_changed;

  dbus_powerd_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusPowerdProxyPrivate));
#endif
}

static void
dbus_powerd_proxy_iface_init (DbusPowerdIface *iface)
{
  iface->get_brightness = dbus_powerd_proxy_get_brightness;
}

/**
 * dbus_powerd_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-canonical-powerd.top_of_page">com.canonical.powerd</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_proxy_new_finish() to get the result of the operation.
 *
 * See dbus_powerd_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_powerd_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_POWERD_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.canonical.powerd", NULL);
}

/**
 * dbus_powerd_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_powerd_proxy_new().
 *
 * Returns: (transfer full) (type DbusPowerdProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusPowerd *
dbus_powerd_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_POWERD (ret);
  else
    return NULL;
}

/**
 * dbus_powerd_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-canonical-powerd.top_of_page">com.canonical.powerd</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusPowerdProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusPowerd *
dbus_powerd_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_POWERD_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.canonical.powerd", NULL);
  if (ret != NULL)
    return DBUS_POWERD (ret);
  else
    return NULL;
}


/**
 * dbus_powerd_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like dbus_powerd_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_powerd_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See dbus_powerd_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_powerd_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_POWERD_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.canonical.powerd", NULL);
}

/**
 * dbus_powerd_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_powerd_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_powerd_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type DbusPowerdProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusPowerd *
dbus_powerd_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_POWERD (ret);
  else
    return NULL;
}

/**
 * dbus_powerd_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like dbus_powerd_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_powerd_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusPowerdProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusPowerd *
dbus_powerd_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_POWERD_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.canonical.powerd", NULL);
  if (ret != NULL)
    return DBUS_POWERD (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * DbusPowerdSkeleton:
 *
 * The #DbusPowerdSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusPowerdSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusPowerdSkeleton.
 */

struct _DbusPowerdSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_dbus_powerd_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], DBUS_TYPE_POWERD);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_POWERD);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_dbus_powerd_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_powerd_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_dbus_powerd_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_powerd_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _dbus_powerd_skeleton_vtable =
{
  _dbus_powerd_skeleton_handle_method_call,
  _dbus_powerd_skeleton_handle_get_property,
  _dbus_powerd_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
dbus_powerd_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return dbus_powerd_interface_info ();
}

static GDBusInterfaceVTable *
dbus_powerd_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_dbus_powerd_skeleton_vtable;
}

static GVariant *
dbus_powerd_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_84
  g_variant_builder_init_static (&builder, G_VARIANT_TYPE ("a{sv}"));
#else
  g_variant_builder_init(&builder, G_VARIANT_TYPE ("a{sv}"));
#endif
  if (_dbus_powerd_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _dbus_powerd_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _dbus_powerd_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _dbus_powerd_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.canonical.powerd", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _dbus_powerd_emit_changed (gpointer user_data);

static void
dbus_powerd_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _dbus_powerd_emit_changed (skeleton);
}

static void
_dbus_powerd_on_signal_sys_power_state_change (
    DbusPowerd *object,
    gint arg_sysState)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(i)",
                   arg_sysState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.canonical.powerd", "SysPowerStateChange",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_dbus_powerd_on_signal_wakeup (
    DbusPowerd *object)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.canonical.powerd", "Wakeup",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void dbus_powerd_skeleton_iface_init (DbusPowerdIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusPowerdSkeleton, dbus_powerd_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (DbusPowerdSkeleton)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_POWERD, dbus_powerd_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusPowerdSkeleton, dbus_powerd_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_POWERD, dbus_powerd_skeleton_iface_init))

#endif
static void
dbus_powerd_skeleton_finalize (GObject *object)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (object);
  guint n;
  for (n = 0; n < 1; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (dbus_powerd_skeleton_parent_class)->finalize (object);
}

static void
dbus_powerd_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_dbus_powerd_emit_changed (gpointer user_data)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_84
  g_variant_builder_init_static (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init_static (&invalidated_builder, G_VARIANT_TYPE ("as"));
#else
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
#endif
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "com.canonical.powerd",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_dbus_powerd_schedule_emit_changed (DbusPowerdSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
dbus_powerd_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _dbus_powerd_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _dbus_powerd_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
dbus_powerd_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 1);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_powerd_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _dbus_powerd_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
dbus_powerd_skeleton_init (DbusPowerdSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = dbus_powerd_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, DBUS_TYPE_POWERD_SKELETON, DbusPowerdSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 1);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_INT);
}

static gint 
dbus_powerd_skeleton_get_brightness (DbusPowerd *object)
{
  DbusPowerdSkeleton *skeleton = DBUS_POWERD_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_int (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
dbus_powerd_skeleton_class_init (DbusPowerdSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = dbus_powerd_skeleton_finalize;
  gobject_class->get_property = dbus_powerd_skeleton_get_property;
  gobject_class->set_property = dbus_powerd_skeleton_set_property;
  gobject_class->notify       = dbus_powerd_skeleton_notify;


  dbus_powerd_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = dbus_powerd_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = dbus_powerd_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = dbus_powerd_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = dbus_powerd_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusPowerdSkeletonPrivate));
#endif
}

static void
dbus_powerd_skeleton_iface_init (DbusPowerdIface *iface)
{
  iface->sys_power_state_change = _dbus_powerd_on_signal_sys_power_state_change;
  iface->wakeup = _dbus_powerd_on_signal_wakeup;
  iface->get_brightness = dbus_powerd_skeleton_get_brightness;
}

/**
 * dbus_powerd_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-canonical-powerd.top_of_page">com.canonical.powerd</link>.
 *
 * Returns: (transfer full) (type DbusPowerdSkeleton): The skeleton object.
 */
DbusPowerd *
dbus_powerd_skeleton_new (void)
{
  return DBUS_POWERD (g_object_new (DBUS_TYPE_POWERD_SKELETON, NULL));
}

