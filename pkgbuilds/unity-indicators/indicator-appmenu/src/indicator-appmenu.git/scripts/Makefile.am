
libexec_PROGRAMS = menu-pusher

menu_pusher_SOURCES = \
	menu-pusher.c
menu_pusher_CFLAGS = \
	$(INDICATOR_CFLAGS) \
	-Wall \
	-Wno-error=deprecated-declarations
menu_pusher_LDADD = \
	$(INDICATOR_LIBS)
